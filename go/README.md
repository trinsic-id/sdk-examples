Go is interesting in how the tests have to be stored, but they are [here](https://github.com/trinsic-id/sdk/tree/main/go/services) in the files `_test.go`.
 * `services_test.go` has the vaccine demo
 * The other files test the named service (eg `account_service_test.go` tests `AccountService`)